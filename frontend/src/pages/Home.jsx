import MapView from "../components/MapView";
import { dummyProperties } from "../data/dummyProperties";

const Home = () => {
  return <MapView properties={dummyProperties} />;
};

export default Home;
